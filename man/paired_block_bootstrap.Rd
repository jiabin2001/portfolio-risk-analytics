% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comparative_evaluation.R
\name{paired_block_bootstrap}
\alias{paired_block_bootstrap}
\title{Paired circular block bootstrap for a loss difference}
\usage{
paired_block_bootstrap(
  loss_difference,
  bootstrap_replications = 999L,
  block_length = NULL,
  confidence_interval = 0.95,
  seed = 20260718L
)
}
\arguments{
\item{loss_difference}{Model score minus reference score, in time order.}

\item{bootstrap_replications}{Number of resamples, at least 99.}

\item{block_length}{Block length, default ceiling(n^(1/3)).}

\item{confidence_interval}{Coverage of the marginal bootstrap interval.}

\item{seed}{Reproducible random seed; caller RNG state is preserved.}
}
\value{
Named inference result. At least 20 observations and two blocks are required.
}
\description{
Blocks retain short-range serial dependence. The percentile interval is marginal;
the two-sided p-value uses bootstrap differences centred at the observed mean.
Inference assumes a sufficiently stable loss-difference process and does not
account for selecting models or hyperparameters using the evaluation sample.
}
